<%= form_with(model: users_sport, local: true) do |form| %>
  <% if users_sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_sport.errors.count, "error") %> prohibited this users_sport from being saved:</h2>

      <ul>
      <% users_sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
