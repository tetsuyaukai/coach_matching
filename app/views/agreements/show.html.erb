<!-- <p id="notice"><%= notice %></p> -->

<div class="container-fluid mb-2 ml-5">
  <%= link_to '戻る', agreements_url(@agreement), class: "btn btn-primary" %>
</div>

<div class="container list-group-item">
  <%= render partial: "contents/detail", locals: {content: @agreement.content} %>
  <p>
    ステータス：<%= @agreement.status %>
    <% if @agreement.status == 'request' %>
        <%= link_to "申請を取り下げる", agreement_url(@agreement, params: {agreement: {status: :request}}), method: :delete %>
    <% end %>
  </p>
</div>
